import {MessageCircle} from 'lucide-react';import {whatsappLink} from '../lib/contact.js';export default function ProductCard({product}){return <article className="card-lux overflow-hidden"><img src={product.image} alt={product.alt} className="h-80 w-full object-cover" loading="lazy"/><div className="p-6"><h3 className="text-3xl">{product.name}</h3><p className="mt-3 text-sm leading-relaxed text-muted">{product.description}</p><p className="mt-5 font-display text-xl text-brass">{product.priceRange}</p><a className="btn-base btn-brass mt-6" href={whatsappLink(`Hello Leaf Fairy, I would like to enquire about ${product.name}.`)} target="_blank" rel="noreferrer"><MessageCircle size={15}/> Enquire about this piece</a></div></article>}
