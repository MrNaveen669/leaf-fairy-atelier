import {useEffect,useState} from 'react';import {Link,NavLink} from 'react-router-dom';import {Menu,X} from 'lucide-react';import {api} from '../api/client.js';export default function SiteHeader(){const[scrolled,setScrolled]=useState(false),[open,setOpen]=useState(false),[collections,setCollections]=useState([]);useEffect(()=>{const onScroll=()=>setScrolled(window.scrollY>24);window.addEventListener('scroll',onScroll);return()=>window.removeEventListener('scroll',onScroll)},[]);useEffect(()=>{api.get('/collections').then(setCollections).catch(()=>{})},[]);const links=[...collections.map(c=>({label:c.title,to:`/collections/${c.slug}`})),{label:'Contact',to:'/contact'}];return <header className={`fixed inset-x-0 top-0 z-50 transition-all ${scrolled?'border-b border-border/80 bg-charcoal/90 backdrop-blur-xl':'bg-transparent'}`}><div className="mx-auto flex h-20 max-w-7xl items-center justify-between px-6 lg:px-10"><Link to="/" className="font-display text-2xl tracking-[.08em]">Leaf Fairy</Link><nav className="hidden items-center gap-7 lg:flex">{links.map(l=><NavLink key={l.to} to={l.to} className="text-[11px] uppercase tracking-[.18em] text-muted hover:text-cream">{l.label}</NavLink>)}</nav><button className="lg:hidden" onClick={()=>setOpen(v=>!v)} aria-label="Toggle menu">{open?<X/>:<Menu/>}</button></div>{open&&<nav className="border-t border-border bg-charcoal px-6 py-6 lg:hidden">{links.map(l=><NavLink onClick={()=>setOpen(false)} key={l.to} to={l.to} className="block border-b border-border py-4 text-sm uppercase tracking-[.18em]">{l.label}</NavLink>)}</nav>}</header>}
