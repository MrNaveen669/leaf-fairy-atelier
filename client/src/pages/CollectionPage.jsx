import {useEffect,useState} from 'react';import {Link,useParams} from 'react-router-dom';import SiteHeader from '../components/SiteHeader.jsx';import SiteFooter from '../components/SiteFooter.jsx';import Reveal from '../components/Reveal.jsx';import ProductCard from '../components/ProductCard.jsx';import {api} from '../api/client.js';export default function CollectionPage(){const{slug}=useParams();const[data,setData]=useState(null),[loading,setLoading]=useState(true),[error,setError]=useState('');useEffect(()=>{setLoading(true);setError('');api.get(`/collections/${slug}`).then(setData).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[slug]);if(loading)return <div className="min-h-screen grid place-items-center">Loading collection…</div>;if(error||!data)return <div className="min-h-screen"><SiteHeader/><main className="grid min-h-[80vh] place-items-center p-8 text-center"><div><p className="eyebrow">Collection not found</p><h1 className="mt-4 text-5xl">This collection is not available.</h1><Link to="/" className="btn-base btn-brass mt-8">Return home</Link></div></main><SiteFooter/></div>;return <div className="min-h-screen"><SiteHeader/><main><section className="relative min-h-[72vh] overflow-hidden flex items-end"><img src={data.image} alt={data.alt} className="absolute inset-0 h-full w-full object-cover"/><div className="veil absolute inset-0"/><div className="relative mx-auto w-full max-w-7xl px-6 pb-20 pt-36 lg:px-10"><p className="eyebrow">{data.kicker}</p><h1 className="mt-5 text-5xl lg:text-7xl">{data.title}</h1><p className="mt-6 max-w-2xl text-muted leading-7">{data.heroLine}</p></div></section><section className="py-24"><div className="mx-auto max-w-7xl px-6 lg:px-10"><Reveal><p className="eyebrow">The Edit</p><h2 className="mt-4 text-4xl">Pieces in this collection.</h2></Reveal><div className="mt-12 grid gap-7 md:grid-cols-2 lg:grid-cols-3">{data.products.map((p,i)=><Reveal key={p._id||p.name} delay={(i%3)*.06}><ProductCard product={p}/></Reveal>)}</div></div></section></main><SiteFooter/></div>}
