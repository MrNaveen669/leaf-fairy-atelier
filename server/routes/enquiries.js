import {Router} from 'express';import {z} from 'zod';import {createEnquiry} from '../controllers/enquiries.js';import {validate} from '../middleware/validate.js';const enquirySchema=z.object({name:z.string().trim().min(2),email:z.string().trim().email(),phone:z.string().trim().min(7),space:z.string().trim().min(1),message:z.string().trim().min(10)});const r=Router();r.post('/',validate(enquirySchema),createEnquiry);export default r;
